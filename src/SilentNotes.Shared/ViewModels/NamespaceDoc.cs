using System;

namespace SilentNotes.ViewModels
{
    /// <summary>
    /// The viewmodels offer the functionality, a view requires to present a model. In contrast to
    /// the models, they are throw away objects, and live only as long as the view is shown.
    /// </summary>
    internal class NamespaceDoc
    {
    }
}
